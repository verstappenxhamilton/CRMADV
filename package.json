{
  "name": "crmadv-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@12.4.1",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=12.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @crmadv/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test:unit": "vitest run",
    "check": "pnpm typecheck && pnpm test:unit"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vitest": "^3.0.7"
  }
}
