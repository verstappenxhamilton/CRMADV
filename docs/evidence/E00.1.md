# Evidência de Entrega — E00.1

- **Resultado:** Workspace monorepo pnpm inicializado com TypeScript strict, Next.js 16.3.5 (App Router), contratos Zod em `@crmadv/contracts`, tokens visuais em `@crmadv/ui`, domínio isolado em `@crmadv/domain`, testes unitários com Vitest e endpoint `/health/live` com schema validado.
- **Arquivos principais:**
  - Configuração: `package.json`, `pnpm-workspace.yaml`, `.npmrc`, `tsconfig.base.json`, `vitest.config.ts`, `.gitignore`, `.env.example`.
  - Pacotes: `packages/contracts/*`, `packages/domain/*`, `packages/ui/*`.
  - Aplicação Web: `apps/web/*`, incluindo validação de ambiente em `src/lib/env.ts` e rota `src/app/health/live/route.ts`.
  - Planejamento: `planning/versions.lock.md`, `planning/external-setup.md`.
- **Correções posteriores de code review (16/09/2026):**
  - `.env*` ignorado por padrão, preservando apenas `.env.example`.
  - Validação de ambiente conectada ao runtime da aplicação.
  - `tsconfig.base.json` tornado neutro quanto a APIs DOM; tipos DOM ficam restritos ao web/UI.
  - Tokens de UI exportados e consumidos pela aplicação.
  - Versão da aplicação centralizada em `@crmadv/contracts`.
  - `uptimeSeconds` passou a usar `process.uptime()`.
  - Vitest passou a descobrir testes `.ts` e `.tsx`.
  - Scripts chamados `lint` que apenas executavam TypeScript foram removidos.
  - Estratégia de versões documentada corretamente: intervalos em `package.json` e resolução exata em `pnpm-lock.yaml`.
  - GitHub Actions CI removido por decisão do mantenedor; validações passam a ser executadas localmente quando necessário.
- **Última validação automatizada antes da remoção do CI:**
  - `pnpm check`: 9 testes passaram.
  - `pnpm build`: Next.js compilou com sucesso.
- **Validação das correções posteriores:**
  - Revisão estática concluída no repositório.
  - Execução local de `pnpm check` e `pnpm build` ainda deve ser feita após sincronizar a branch `comeco`, pois o ambiente desta revisão não possui acesso de rede ao GitHub para clonar e executar o projeto.
