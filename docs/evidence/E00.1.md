# Evidência de Entrega — E00.1

- **Resultado:** Workspace monorepo pnpm inicializado com TypeScript strict, Next.js 16.3.5 (App Router), contratos Zod em `@crmadv/contracts`, tokens visuais em `@crmadv/ui`, domínio isolado em `@crmadv/domain`, testes unitários com Vitest e endpoint `/health/live` respondendo HTTP 200 com schema validado.
- **Arquivos:**
  - Configuração: `package.json`, `pnpm-workspace.yaml`, `.npmrc`, `tsconfig.base.json`, `vitest.config.ts`, `.gitignore`, `.env.example`, `.github/workflows/ci.yml`.
  - Pacotes: `packages/contracts/*`, `packages/domain/*`, `packages/ui/*`.
  - Aplicação Web: `apps/web/*` (incluindo `src/app/health/live/route.ts` e `page.tsx`).
  - Planejamento: `planning/versions.lock.md`, `planning/external-setup.md`.
- **Validação:**
  - `pnpm check` (typecheck em todos os workspaces + vitest run): 7 testes passaram (100% sucesso).
  - `pnpm build`: compilação Turbopack do Next.js gerou páginas estáticas e rota dinâmica `/health/live`.
  - HTTP Test (`GET http://localhost:3005/health/live`): retornou `{ status: "ok", version: "0.1.0" }`.
- **Pendências:**
  - Nenhuma para a subetapa E00.1. Próximo passo autorizado pela supervisora: E00.2 / E01.
